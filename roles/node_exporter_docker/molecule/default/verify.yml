---
- name: Verify
  hosts: all
  gather_facts: false

  tasks:
    - name: Get info on node-exporter container
      community.docker.docker_container_info:
        name: node-exporter
      register: con_info

    - name: Verify container exists
      ansible.builtin.assert:
        that: con_info.exists

- import_tasks: install.yml
  tags:
    - telegraf
    - telegraf:install

- import_tasks: config.yml
  tags:
    - telegraf
    - telegraf:config

- import_tasks: plugin_docker.yml
  tags:
    - telegraf
    - telegraf:plugins
    - telegraf:plugins:docker
  when: telegraf_plugin_docker

- import_tasks: plugin_smartctl.yml
  tags:
    - telegraf
    - telegraf:plugins
    - telegraf:plugins:smartctl
  when: telegraf_plugin_smartctl

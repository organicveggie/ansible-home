---
- name: Set login banner
  copy:
    src: motd.txt
    dest: /etc/motd

- name: Enable Universe repository
  raw: apt-add-repository universe
  register: add_repo_result
  changed_when: "'distribution component enabled for all sources' in add_repo_result.stdout"
  until: add_repo_result.rc == 0
  retries: 3
  delay: 1

- name: Update apt-cache
  apt:
    update_cache: yes
    cache_valid_time: 3600
  register: result
  until: result is succeeded

- name: Upgrade all packages
  apt:
    upgrade: yes
    state: latest
  when: keep_packages_updated
  tags:
    - skip_ansible_lint

- name: Install some packages
  apt:
    name: "{{ veggie_nas_extra_packages }}"
    state: present
  register: result
  until: result is succeeded

- name: "Set hostname to {{ veggie_nas_hostname }}"
  hostname:
    name: "{{ veggie_nas_hostname }}"

- name: "Set timezone to {{ veggie_nas_timezone }}"
  timezone:
    name: "{{ veggie_nas_timezone }}"

- name: Create veggie-nas group
  group:
    name: veggie-nas
    state: present

- name: Create veggie-nas user
  user:
    name: veggie-nas
    state: present
    system: yes
    update_password: on_create
    create_home: no
    group: veggie-nas
    shell: /usr/sbin/nologin
---
- name: "calibre-web | Create groups"
  ansible.builtin.group:
    name: "{{ r740xd_calibre_web_groupname }}"
    gid: "{{ r740xd_calibre_web_gid }}"
    system: "false"
    state: "present"

- name: "calibre-web | Create users"
  ansible.builtin.user:
    append: true
    create_home: false
    group: "{{ r740xd_calibre_web_groupname }}"
    groups:
      - "media_writer"
    name: "{{ r740xd_calibre_web_username }}"
    password: "*"
    password_lock: true
    shell: "/bin/false"
    state: "present"
    system: false
    uid: "{{ r740xd_calibre_web_uid }}"

- name: "calibre-web | Setup Docker network"
  community.docker.docker_network:
    name: "{{ docker_networks.calibre_web.name }}"
    driver: "bridge"
    enable_ipv6: "no"
    internal: "no"
    scope: "local"
    ipam_config:
      - subnet: "{{ docker_networks.calibre_web.subnet }}"
        gateway: "{{ docker_networks.calibre_web.gateway }}"

- name: "calibre-web | Create container"
  ansible.builtin.import_role:
    name: "organicveggie.calibre_web_docker"
  vars:
    calibre_web_docker_network_create: false
    calibre_web_docker_book_dir: "{{ r740xd_calibre_web_book_dir }}"
    calibre_web_docker_extra_networks:
      - name: "{{ docker_networks.calibre_web.name }}"
    calibre_web_docker_gid: "{{ r740xd_calibre_web_gid }}"
    calibre_web_docker_host_domain: "{{ r740xd_calibre_web_host_domain }}"
    calibre_web_docker_hostname: "{{ r740xd_calibre_web_hostname }}"
    calibre_web_docker_volume_name: "calibre-web-config"
    calibre_web_docker_uid: "{{ r740xd_calibre_web_uid }}"
